name: Build Chocolatey Packages

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  build:
    runs-on: windows-latest

    env:
      "api_key": ${{ secrets.CHOCO_API_KEY }}
      "gist_id": ${{ secrets.GIST_ID }}
      "gist_id_test": ${{ secrets.GIST_ID_TEST }}
      "github_user_repo": ${{ secrets.GH_USER_REPO }}
      "github_api_key": ${{ secrets.GH_API_KEY }}
      "au_Push": ${{ secrets.AU_PUSH }}
      "actions_test": ${{ secrets.ACTIONS_TEST }}

    steps:
      - name: Checkout repository
        uses: actions/checkout@v2

      - name: Set up Chocolatey
        run: |
          Set-ExecutionPolicy Bypass -Scope Process -Force; iex ((New-Object System.Net.WebClient).DownloadString('https://chocolatey.org/install.ps1'))

      - name: pwsh-test
        shell: pwsh
        run: |
          Write-Host "$env:actions_test"

      - name: setup au
        run: |
          .\setup\au_setup.ps1

      - name: update and push all packages
        run: |
          .\update_all.ps1
